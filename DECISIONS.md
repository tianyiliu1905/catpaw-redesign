# 设计决策记录

逐条记录，不做归纳。新决策追加到末尾。

---

## 1. 侧边栏的分组叫「文件夹」，不叫「项目」或「空间」

**决策**：侧边栏中承载任务的分组，统一称为「文件夹」。

**理由**：
- 「项目」隐含了目标、周期和完成态，会让用户误以为需要先规划再使用，心智负担重。
- 「空间」偏概念化，用户不清楚自己该往里放什么，也不知道和别的空间边界在哪。
- 「文件夹」是操作系统里最通用的容器隐喻，零学习成本，用户天然知道可以随手建、随手扔东西进去、随手删。
- 与输入框下方的归属选择器（「默认文件夹」）用词一致，同一个概念在产品里只有一种叫法。

**影响范围**：侧边栏分组标题、输入框下方的归属选择器、工具台的「文件夹」tab。

---

## 2. 文件面板中点击 HTML，在原地预览，不跳转到浏览器

**决策**：文件面板是一个预览容器，点开任何文件都在当前面板内打开，由容器根据文件类型挂载对应的渲染器。HTML 复用浏览器内核能力，但不呈现浏览器外壳。

**类型与渲染器对应关系**：

| 文件类型 | 渲染器 | 形态 |
|---|---|---|
| `.md` | Markdown 渲染 | 排版后的富文本 |
| `.js` `.css` `.py` | 代码高亮 | 只读编辑器 |
| `.png` `.jpg` | 图片查看器 | 适应容器 + 缩放 |
| `.docx` `.pptx` | 文档渲染 | 分页视图 |
| `.html` | webview | 渲染后的页面 |

**理由**：
- HTML 不作为特例处理，它只是众多渲染器中恰好使用浏览器内核的那一个，架构上可扩展，也避免为单一格式开后门。
- 用户的心智模型应是「打开了一个文件」，而非「跳进了浏览器」。文件和它的预览待在同一上下文里，比切换到浏览器 tab 更符合直觉。
- 与工具台「浏览器」tab 的区别不在内核，而在外壳（chrome）：
  - 浏览器 tab = webview + 完整外壳（地址栏、多标签、标签总览）
  - 文件预览 = webview + 零外壳，至多一条极简工具条（刷新 / 缩放 / 在浏览器中打开）
- 底层同一能力，差异仅在外壳，属于复用而非新增开销。

**技术可行性（已实测）**：

| 场景 | 渲染 | 读取内部 DOM |
|---|---|---|
| 父子均为 `file://` | 空白 | 跨源，不可读 |
| 父 `file://`，子 http | 空白 | 跨源，不可读 |
| 父子均为本地 http | 正常 | 可读 |

结论：方案成立，卡点仅在协议。纯静态 `file://` 形态下无法内嵌；需依托本地 dev server，或打包为 Electron / Tauri。Electron 下可直接使用 `<webview>`，不受同源限制，并可独立控制导航与缩放。

**待与研发对齐的细节**：
1. 相对资源解析：HTML 常引用同目录的 css/js/图片，baseURL 不正确会白屏或掉样式，直接决定走文件路径还是本地服务。
2. 交互边界：预览内的 JS 是否执行；点击站外链接是在预览内跳转，还是移交工具台的浏览器 tab（倾向后者，职责更清晰）。
3. 安全沙箱：是否限制预览页访问本地文件与网络，需与产品既有的「安全屋 / 完全访问」权限模型对齐。
4. 状态保持：切走再切回时，预览是重新加载还是保活。含动画或已填表单的页面，重载体验较差。
5. 宽度适配：460px 面板承载桌面版 HTML 会变形，需确定采用等比缩放预览，还是支持拖拽调宽。

---

## 3. 任务列表的视觉语言：左对齐表达层级，右对齐表达状态

**决策**：任务项建立两条垂直轴线。左轴承载层级，右轴承载状态。

**左轴 —— 用缩进表达归属，不用图标**
- 不在文件夹里的任务左侧顶头，与「任务」区块标题同一起点。
- 文件夹内的任务缩进一级。
- 归属关系完全由空间位置传达，不额外引入图标或连线。

**右轴 —— 状态与时间互斥，同一位置只出现一个元素**

| 状态 | 右侧呈现 |
|---|---|
| running | 转圈指示器（不显示时间） |
| new | 主题绿圆点 |
| await | 「待确认」标签（主题绿） |
| 其余 | 相对时间（如 `5h`、`2d`） |

运行中的任务不显示时间：「已进行多久」在此时意义不大，转圈本身已传达了状态。

**溢出处理 —— 只在真正被裁切时加渐隐**
- 任务名溢出才在右侧加渐隐遮罩，未溢出的保持完整收尾，不无差别加遮罩。
- 需在字体异步加载完成后、以及窗口 resize 后重新检测（字体未就位时宽度测量不准）。
- 检测容差 1px，规避亚像素导致的误判。

**理由**：
- 左对齐的任务名形成稳定的阅读起点，眼睛扫描时不需要在不同缩进间跳跃。
- 状态收归右侧独立一列，使「读内容」和「看状态」成为两个独立动作，互不干扰。
- 遮罩是异常态的提示，无差别使用会让正常收尾的文字看起来也像被截断，削弱其信息量。

---

## 4. 场景区分 —— 方案一：左侧用颜色，右侧用颜色 + 标语

**背景**：场景选择器（办公 / 开发 / 设计）位于左侧面板。若右侧主区域不做任何区分，用户在折叠左侧面板后将失去场景感知，不知道自己当前身处哪个场景。

**决策**：左右两侧建立不同强度的场景标识，确保左侧折叠时右侧仍可独立传达场景。

**左侧 —— 仅用颜色**
- 场景专属导航项的 icon 染对应场景色，通用项（`data-scope="all"`）保持中性灰。
- 场景色：办公 `#3b72e8`（蓝，秩序、信赖）／开发 `#1fbb8c`（翠绿偏青，终端、构建通过）／设计 `#ea4c89`（粉，创意、表达）。

**右侧 —— 颜色 + 标语**
- 标语句式固定为「__好，想__点什么？」，仅动词随场景变化：办公「推进」／开发「构建」／设计「创作」。
- 整句作为稳定锚点，切换时只有动词做淡出淡入，视觉抖动最小。
- 动词附带场景专属装饰动效，承载颜色维度：办公为闪烁文本光标／开发为分段编译进度条／设计为马克笔涂鸦笔触。

**理由**：
- 场景是一种持续存在的上下文，而非一次性选择，需要在主视野内有常驻表征。
- 颜色提供快速的前注意识别，标语提供明确的语义确认，两者叠加后即使只瞥一眼也能确定场景。
- 动词比名词更能传达「即将做什么」，与产品「描述需求、输出结果」的主动作契合。

**遗留**：本条为方案一。若后续出现方案二（如在主区顶部增加常驻场景标识），需与本条并列记录后再做取舍。

---

## 5. 输入框下方的归属选择器与侧边栏文件夹共用同一数据源

**决策**：归属选择器不是静态文案，其选项来自与侧边栏文件夹相同的一份数据，并随场景过滤。

**数据结构**：单一数据源 `FOLDERS`，每个文件夹含 `id` / `name` / `scope` 三个字段。侧边栏分组通过 `data-folder` 与之对应，保证两处永不出现文案或范围的偏差。

**默认文件夹的定位**：
- `scope` 为 `null`，不归属任何场景，因此在所有场景下都可选。
- 恒定排在选项首位，与场景文件夹之间以分隔线区隔。
- 作为兜底容器，不可删除。用户未做选择时，任务即落入此处。

**行为规则**：
- 选项随场景过滤：当前场景的文件夹 + 默认文件夹。
- 每个场景各自记住上次的选择，切回时不丢失。
- 若切换场景后原选中项已不在可选范围内，自动回落到默认文件夹。

**理由**：
- 「新任务存到哪里」与「侧边栏里看到的文件夹」必须是同一批对象，否则用户会认为存在两套互不相干的文件夹，心智模型破裂。
- 默认文件夹的存在使用户无需在提问前先做分类决策，降低启动成本；同时保证任何任务都有归属，不会出现游离状态。
- 按场景记忆选择，符合「用户在某个场景下通常持续往同一处投放」的实际使用节奏。

**实现注记**：输入框工具条 `z-index: 0` 自成层叠上下文，其内部元素无论设多高的层级都无法越过输入框白卡（`z-index: 1`）。下拉向上弹出时会被白卡遮挡并拦截点击，因此需在展开态抬升工具条自身，而非抬升下拉菜单。

---

## 6. 侧边栏的行操作只在 hover 时出现，且不改变行的布局

**决策**：任务与文件夹的操作入口常驻于 DOM，但仅在 hover / focus 时可见。

| 行类型 | hover 时右侧呈现 |
|---|---|
| 任务 | 三点（更多） |
| 文件夹 | 新建任务 + 三点（更多） |

**与状态的关系 —— 遮盖，而非顶替**

决策 3 已确立「右轴承载状态」。行操作落在同一条右轴上，与状态争用同一段空间，处理方式是：hover 时状态区 `visibility: hidden`（隐藏但不撤位），操作区以绝对定位浮在其上。

之所以不让两者在流内互换，是因为状态列的实际宽度并不齐平：「待确认」tag 约 48px，而绿点与时间戳只有 26px。若直接顶替，await 行的任务名会在 hover 瞬间变宽 22px，文字随指针进出反复抽动。改为浮层后，行内所有盒子的尺寸全程恒定，实测 hover 前后任务名宽度不变。

文件夹标题没有状态列，两颗按钮共 50px，因此改为在 hover 时给标题让出等宽的右内边距。文字左端不动，仅超长时提前收进省略号，同样不产生位移。

**为什么两类行共用同一套按钮**

尺寸（24px）与右边距（6px）完全一致，任务、子任务、文件夹的操作按钮因此落在同一条垂直轴上（实测右边缘均为 302px）。指针在列表里上下移动时，目标位置可预测，不需要为不同行型重新定位。

**文件夹的按钮顺序：新建任务在左，更多在右**

「新建任务」是高频的内容动作，靠近内容一侧；「更多」是低频的管理动作，压在最外侧边缘。最外侧位置对所有行型保持一致，扫视时「更多」永远在同一个地方。

**实现约束**

1. **不能用 `display: none` 收起**。该状态下元素无法获得焦点，键盘用户将永远 Tab 不到这些按钮。改用 `opacity: 0` + `pointer-events: none`：前者隐藏，后者让指针照常穿透到下方的行上。
2. **文件夹标题需拆成两层**。原本整行是一个 `<button>`，按钮内不能再嵌按钮。现由外层 `div.group-head` 承载底色与操作区，内层 `button.group-head-main` 承载折叠触发。副产品是点击操作按钮不会再顺带折叠文件夹。
3. **任务是 `<a>`，操作按钮用 `span[role=button]`**。HTML 不允许在链接内嵌套可交互元素。相应地需手动补上 Enter / Space 的键盘语义，并在点击时 `preventDefault` + `stopPropagation`，否则会连带触发「打开任务」。

---

## 7. 工具台的文件面板跟随当前文件夹，内容以缩略图宫格呈现

**决策之一 —— 面板展示的目录就是输入框下方选中的那个文件夹**

此前面板里是写死的工程目录（`catpaw-desktop-ui / src`），与用户的任何选择都无关。现改为：位置与内容均由当前归属文件夹渲染，换文件夹、换场景都会同步。

这是决策 5「归属选择器与侧边栏文件夹共用同一数据源」的延续。同一份 `FOLDERS` 现在供给三处：侧边栏分组、归属选择器、文件面板。新增 `path`（磁盘位置）与 `files`（内容）两个字段。

若面板与选择器各指各的，用户会认为「我存任务的地方」和「我在文件面板里看到的地方」是两个不同的目录，决策 5 建立的单一心智会被重新打破。

**决策之二 —— 工具条只写当前文件夹名，不铺完整路径**

原先用面包屑展示 `path` 的每一层。但上层目录并不是这个面板能去的地方：面板展示哪个文件夹，由输入框的归属选择器决定，面板本身无权代为跳转。把不可达的层级铺出来，等于给出一排点了没反应的按钮。

因此工具条只留当前所在那一层的名字，层级移动交给左侧的前进 / 后退——与浏览器面板同形同位，是一套手势而非两套。

**决策之三 —— 用缩略图宫格，不用行列表**

| | 行列表 | 缩略图宫格 |
|---|---|---|
| 识别方式 | 先读文件名，再看小图标 | 先看缩略图，再读文件名 |
| 单屏容量 | 更多 | 更少 |

选宫格。文件夹里主要是文档、表格和图片，这类内容的视觉特征本身就是最快的识别线索；13px 的单色线性图标无法承载这个信息，只是在文件名旁边重复了一次扩展名。460px 宽度下宫格为 4 列，容量损失可以接受。

**缩略图的抽象层级：画「内容形态」，不画「软件品牌」**

不使用 Word / Excel / Figma 之类的品牌标志，因为同一份 `.docx` 可能来自任何工具，绑定品牌既不准确也会让界面变花。改为四类抽象：

| 类型 | 图形 | 关键特征 |
|---|---|---|
| 文件夹 | 双层实心蓝 | 后层露出一条，做出「一沓」的厚度 |
| 文档 | 白色纸张 + 段落骨架 | 首行加深当标题，每三行短一截模拟段落收尾 |
| 表格 | 白色纸张 + 色块网格 | 首行加底纹当表头，两道白缝切出列 |
| 代码 | 白色纸张 + 错落短线 | 缩进不齐是代码区别于正文的唯一线索 |
| 图片 | 渐变画面 + 远山与太阳 | 直接画「一张图」，而非画一个相机图标 |

文档、表格、代码共用同一张纸张底板，只有内部骨架不同。右上角的折角是「纸」的通用符号，去掉它就只剩一个白方块。

**排版细节**

- 缩略图统一占 64px 高的等高方框，内部图形各自居中。方框等高，文件名才会排在同一条基线上，宫格不参差。
- 文件名居中、最多两行、超出省略。居中是因为缩略图是视觉锚点，文字居于其下才与之成为一组。
- 文件夹排在文件之前，与 Finder 的默认排序一致。

**实现注记**：渐变必须在宫格顶部统一定义一次。若把 `<linearGradient>` 写进每个缩略图内部，一屏几十个元素就会产生几十个同名 `id`，而 HTML 不允许 `id` 重复——浏览器只认第一个，其余全部失效，表现为除第一个之外的文件夹图标全是空白。承载定义的 `<svg>` 需设为零尺寸并绝对定位，否则它会在网格里占掉一个格子。

**决策之四 —— 左箭头是「上一级」，不是「上一次访问」**

点击子文件夹进入下一层，左箭头逐层退回。`files` 里 `type: folder` 的项可再带 `files`，结构是递归的，渲染时只看当前节点这一层，因此嵌多少层都由同一套代码处理。

左箭头有两种可能的语义：结构上的「父目录」，或浏览器式的「上一个访问过的页面」。这里选前者。图标是一支向左的箭头，紧挨着的位置显示的是当前目录名，这个组合已经把它读成了「往上走」。若做成访问历史，从 `A/B` 切到 `C/D` 后按下左箭头会跳回 `A/B`——一次跨树的横向跳转，与图标给出的暗示相反。

右箭头则是左箭头的逆操作：撤销刚才的后退。它必须是一个栈而非单个节点，否则连退两层后只能前进一层，深的那一层再也找不回来。一旦从别处重新下钻，这条待重做的路径整条作废。

无处可去时按钮置灰并 `disabled`。置灰是为了让这件事在按下之前就被看到；`disabled` 则顺带把它们移出 Tab 序列，键盘不会停在一个走不通的目标上。CSS 里额外关掉 `pointer-events`，避免出现「看起来能按、按下去没反应」的 hover 反馈。

换归属文件夹等于换了一棵树，层级栈因此重置到根：此前停留的那个子目录已不属于新树，不重置的话面板会停在一个与当前选择无关的目录上，决策之一建立的「选择器与面板指向同一对象」会被打破。

**实现注记**：宫格里文件夹优先的排序打乱了与原数据的下标对应，因此渲染时需把原下标写入 `data-file-index`，点击时回查节点。不能拿文件名去 `find`——同一层里出现同名项时会拿错。

---

## 8. 工具台的页签顺序：文件夹 → 浏览器 → 终端

**决策**：文件夹排在最左，浏览器与终端依次在后。此前文件夹夹在中间。

**理由 —— 顺序表达的是「谁是目的，谁是手段」**

三个页签并非平级的三个工具：

| 页签 | 角色 |
|---|---|
| 文件夹 | 任务的产物落点，用户最终要带走的东西 |
| 浏览器 | 过程工具，查资料、看效果 |
| 终端 | 过程工具，跑命令、看日志 |

用户来这个产品是为了得到文件夹里的那些东西，浏览器和终端只是抵达它的途径。把结果放在第一位，过程排在其后，页签顺序因此与「产物 → 手段」的关系一致。

这也与决策 1、5、7 建立的那条主线相衔接：文件夹是贯穿侧边栏、归属选择器、文件面板的核心对象，它在工具台里同样应当处于起始位置，而不是被两个过程工具夹在中间。

**顺序与默认项是两件事**

调整顺序不等于改变默认打开哪一页。排列顺序回答「它们之间是什么关系」，默认项回答「刚进来时先看什么」，两者可以不一致，不应混为一谈——所以下面的默认项是单独一条决策，而非本条的推论。

---

## 9. 工具台默认打开「文件夹」，开与关共用最右侧那一颗按钮

**决策之一 —— 默认页签从「浏览器」改为「文件夹」**

打开工具台的动作，绝大多数时候是为了「看看做出来的东西」，而不是「我要上网」。浏览器与终端都是被某个具体意图召唤的：要查资料才开浏览器，要看日志才开终端；而文件夹是没有特定意图时的默认去处——不确定要干什么的那次点击，落在文件夹上的命中率最高。

默认落在浏览器还有一个副作用：面板一展开就是一个空白页和一条待输入的地址栏，等于反过来向用户要一个输入。而文件夹展开即有内容，是「给」而不是「要」。

与决策 8 的关系：页签顺序（决策 8）与默认项（本条）各自回答不同的问题，只是这次两者的结论恰好一致——文件夹既排第一，也默认选中。

**决策之二 —— 取消单独的关闭叉，开关合并为一颗按钮**

原先是两颗：主区顶栏右端一颗开，面板顶栏一颗叉关。合并为一颗常驻的 toggle。

| | 两颗（开 + 叉） | 一颗 toggle |
|---|---|---|
| 关闭时指针要移动 | 是，从开的位置移到叉的位置 | 否，原地再按一次 |
| 需要记住的位置 | 两个 | 一个 |
| 面板顶栏 | 被一个与内容无关的叉占用 | 完整留给内容 |

开与关是同一件事的两个方向，理应发生在同一个坐标上。反复开合时指针不必来回移动，肌肉记忆只需建立一次。

**实现注记 —— 按钮必须锚在窗口右缘，而不是主区右缘**

这是合并后唯一的技术关卡：按钮若留在主区顶栏内，面板展开会挤窄主区，按钮跟着往左缩进到屏幕中间，「原地再按一次」当场失效——位置变了的按钮不算同一个坐标。

因此把它移出 `.main-topbar`，改为 `.content` 的直接子节点（`.wb-toggle-slot`），定位到窗口右缘。`.content` 需补 `position: relative` 作为定位上下文。实测面板开合两态下，按钮距窗口右缘恒为 38px，与工具台顶栏右端对齐。

---

## 10. 任务的三点菜单：全局单实例 + fixed 定位

**决策之一 —— 菜单项与顺序**

决策 6 已经确立任务行 hover 时只出一颗三点，作为唯一的次级动作出口。这颗三点现在展开一张四项的菜单：

| 项 | 分组 | 说明 |
|---|---|---|
| 置顶 | 定位类 | 改变它在列表中的位置 |
| 在文件夹显示 | 定位类 | 改变你在文件系统中找到它的位置 |
| 重命名 | 改写类 | 改变任务自身 |
| 删除任务 | 改写类 | 改变任务自身，且不可撤销 |

前两项都在回答「它在哪」，后两项都在回答「它是什么」，两组之间用一道分隔线切开。分隔线由 `.row-menu-item[data-divider]::before` 生成，而不是额外插一个空元素——分隔线是「这一项与上一项不同组」的属性，挂在项上比单独立一个节点更贴合它的语义，重排顺序时也不会留下孤立的线。

删除单独标红（文字、图标、hover 底色一并转淡红）。它是四项里唯一不可撤销的，必须在按下之前就与其余项区分开，而不是按下之后再用一个确认弹窗去补救。

**决策之二 —— 菜单挂在 `<body>` 下，用 `position: fixed`**

不把菜单塞进任务行内部。侧边栏是个 `overflow` 滚动容器，菜单若是行的子节点，超出容器的部分会被直接裁掉——列表底部那几行的菜单只会剩上半截。挂到 `<body>` 下并用 fixed 定位，可见性就与任何祖先的裁剪无关了。

全局只建一个实例。菜单内容对每一行都一样，差别只在「它此刻作用于谁」，这一点用一个 `menuRow` 变量记着即可，不必为每行各留一份 DOM。

**定位规则**：右缘对齐触发按钮的右缘，向下展开 4px。左侧装不下时改为左对齐，下方装不下时翻到按钮上方，且与视口始终保持 8px 余量——贴边时宁可换方向，也不让菜单顶出视口。

**实现注记 —— 三个必须踩到的点**

1. **量尺寸前要先放出来**。`hidden` 的元素量到的宽高是 0，贴边判断因此永远不成立。顺序是：`hidden = false` → `visibility: hidden` → 测量 → 定位 → 放开 `visibility`，用户看不到中间态。
2. **测量期间要抹平 `transform`**。入场动画的 `scale(.97)` 会一并作用在测量结果上，量到的宽度比实际小 3%，右对齐会因此偏出几个像素。测量前置 `transform: none`，量完交还给 CSS。修掉之后实测右缘偏差为 0。
3. **「点别处关闭」要走捕获阶段**。三点的点击处理里有 `stopPropagation`（否则会连带触发「打开任务」），冒泡阶段的监听收不到那次点击，从一行切到另一行时旧菜单关不掉。

**滚动即关闭，不重算位置**。fixed 定位的菜单一滚动就脱离了它所属的那一行。与其追着重算，不如直接关掉——滚动本身就意味着用户已经离开了当前上下文。监听需走捕获阶段，否则滚的是侧边栏内部容器时收不到事件。

**菜单展开期间，那一行保持显形**（`.menu-open`）。指针移到菜单上就离开了行，hover 随之失效，若不额外驻留，行会在菜单还开着的时候淡出，「这张菜单属于谁」就断了线索。

---

## 11. 产品模式与场景筛选分成两级，智能掌柜不继承通用场景工具

**决策**：在 CatPaw 品牌名右侧设置紧凑的产品模式下拉框，包含「通用」和「智能掌柜」。原有的「办公 / 开发 / 设计」保留为通用模式下的场景 Tab，而不是与智能掌柜并列。

**联动规则**：
- 通用模式显示场景 Tab，并按所选场景展示对应的彩色专属工具。
- 智能掌柜模式隐藏场景 Tab，同时隐藏依赖这些场景的彩色专属工具。
- 从智能掌柜切回通用时，恢复用户离开前选中的场景，不重置上下文。

**理由**：产品模式回答「当前使用哪套能力」，场景 Tab 回答「通用能力服务于哪类工作」，二者不是同一层级。智能掌柜本身是独立工作模式，不应继续暴露通用模式下的办公、开发、设计切换入口。

---

## 12. 案例区与输入框同宽，一行四张；详情统一进入同一弹窗

**决策**：首页案例区使用与输入框一致的 700px 内容宽度，一行固定展示四张等宽案例卡。所有案例复用同一个详情弹窗，而不是各自维护详情结构。

**弹窗信息结构**：
- 左侧展示案例视觉预览。
- 右侧依次展示标题、产物类型与完整 Prompt。
- 右下角提供唯一主操作「做同款」。
- 弹窗使用固定定位并在视口水平、垂直居中。

**理由**：案例是可直接复用的成品，不只是静态装饰。四张同排可提升首屏浏览效率；统一弹窗保证不同场景、不同 Prompt 分类的案例拥有一致的信息层级和操作路径，也避免重复 DOM 与行为分叉。

---

## 13. Prompt 推荐采用“一级意图 → 二级具体任务 → 相关案例”的渐进式链路

**决策**：首页四个 Prompt 是一级意图分类。点击后，其他一级项淡出，当前分类进入选中状态，并同步展示该分类的二级 Prompt 与相关案例；再次点击当前一级分类则取消选择、隐藏二级推荐并恢复默认案例。

**输入规则**：
- 一级 Prompt 不直接改写 textarea，而是在输入内容之前生成一个可清除、可替换的 Tag。
- Tag 使用缩短后的分类文案，完整引导语仍作为提交上下文保留。
- 二级 Prompt 作为普通文字填入 textarea，允许用户继续编辑。
- Tag 与 textarea 文字使用一致的 14px 字号和 22px 行高，在同一输入行顶端对齐。
- 只有 Tag、只有正文，或两者同时存在时，都应正确更新发送按钮状态；提交时按「引导语 + 正文」组合。

**视觉规则**：
- 二级 Prompt 与一级 Prompt 保持同一字号层级，避免选中后信息突然降级。
- 二级推荐始终单行展示，超出容器时裁切，并在右侧增加向页面背景过渡的渐变遮罩，明确提示后方仍有内容，同时避免生硬截断。
- 推荐项和案例更新使用短暂淡出、淡入或轻微上浮；在 `prefers-reduced-motion` 下取消动画。

**理由**：一级 Prompt 用于建立意图，二级 Prompt 用于补全具体任务，案例则展示可能的结果。Tag 将系统提供的引导语与用户自由输入区分开，不侵占用户文本，也允许随时撤销。

---

## 14. 首页主体采用有意的下移构图，并压缩推荐与案例之间的空白

**决策**：首页主内容不做几何绝对居中，而是整体向下偏移：常规窗口使用较明显的下移量，短屏窗口仍保留较小下移量；与此同时，将 Prompt 推荐区与案例区之间的垂直留白控制在较紧凑的范围。

**理由**：输入框右上方有向上探出的装饰图，若整组严格居中，视觉重心会偏高。增大标题上方留白、缩小内容组下方留白，可以让标题、输入框、推荐和案例形成一个稳定整体；推荐与案例之间也不应远到像两个无关模块。

---

## 15. 输入框装饰图以参考 PNG 为视觉真值，折叠侧边栏时同步缩小

**决策**：不再用近似手绘 SVG 重构输入框右上角装饰，而是直接使用用户确认的透明 PNG。原图以 204 × 112 的固有比例保存为项目本地资源；展开状态显示为 146 × 80px，侧边栏折叠时缩小为 102 × 56px。

**定位规则**：
- 装饰图位于输入卡右上方、卡片内容层之后。
- 展开与折叠两态下，图片下沿都与输入卡上沿保持 0px 无缝衔接。
- 折叠后不仅横向重定位，也同步缩小，避免主内容变宽后装饰图显得过重。

**理由**：该装饰属于具有明确造型的品牌视觉，轮廓、描边和透明边缘都需要像素级一致。继续手工描摹 SVG 会引入可见偏差；直接使用确认过的 PNG 更忠实，也能通过项目内相对路径保持离线可分发。

---

## 附：交互验证记录

决策 7、9、10 涉及的交互已在浏览器中逐项验证：

| 验证项 | 结果 |
|---|---|
| 默认页签 | 展开后 `activePane = files`，位置显示「默认文件夹」 |
| 开关合并 | `#wbClose` 已不存在；按钮距窗口右缘恒为 38px |
| 文件夹下钻 | 进入草稿箱（4 项）→ 左箭头回到默认文件夹（3 项）→ 右箭头重回草稿箱；两端按钮的 `disabled` 随层级正确翻转 |
| 菜单内容 | 置顶 / 在文件夹显示 / 重命名 / 删除任务 |
| 菜单定位 | 右缘偏差 0px，下沿间距 5px，完整落在视口内 |
| 关闭路径 | 再点同一行、切到另一行、Esc、点空白、滚动，五条路径均正确关闭且无 `.menu-open` 残留 |
